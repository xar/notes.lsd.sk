horizon:
  links:
    - rethinkdb
application:
  image: registry.rainbowcrown.com:7443/root/simpleeditor:app-next
  command: npm run serve
  expose:
    - "80"
rethinkdb:
  image: rethinkdb
  restart: always
  #labels:
  #  io.rancher.scheduler.affinity:host_label_soft: name=batman.lsd.sk
  #  io.rancher.scheduler.affinity:host_label: name=batman.lsd.sk
  volumes:
    - /docker/notes.preview.lsd.sk/rethinkdb:/data
